// - створити функцію яка створює параграф з текстом. Текст задати через аргумент


function textParagraph (paragraph) {
    document.write(`<p>${paragraph}</p>`);
}


textParagraph(`hello world`);

textParagraph(`dogs are amazing!`);

textParagraph(`cats are smaller than dogs`);